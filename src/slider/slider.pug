include slider__slider-view/slider__slider-view.pug
mixin slider(posts)
    .slider.page__slider
        each post in posts
            +slider__slider-view(post.title, post.category_tag, post.user, post.date, post.comments, post.views, post.preview_url)
    .page__dots.dots
