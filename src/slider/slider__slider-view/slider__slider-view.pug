mixin slider__slider-view(title, tag, user, date, comment_count, views, url)
    .slider__slider-view(style="background-image: url(" + url  + ")")
        +post('l', title, tag, user, date, comment_count, views, url)
